name: Compilar Velyntora APK

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-android:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v5

      - name: Instalar Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Crear soporte de Android
        run: flutter create . --platforms=android --project-name=velyntora --org=art.arcanokuro

      - name: Descargar dependencias
        run: flutter pub get

      - name: Aplicar icono de Velyntora
        run: dart run flutter_launcher_icons

      - name: Aplicar formato oficial
        run: dart format lib test

      - name: Guardar formato corregido
        shell: bash
        run: |
          if ! git diff --quiet -- lib test; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add lib test
            git commit -m "style: apply canonical Dart formatting"
            git push
          fi

      - name: Analizar código
        run: flutter analyze

      - name: Ejecutar pruebas
        run: |
          for test_file in test/*_test.dart; do
            echo "Ejecutando $test_file"
            timeout 90s flutter test "$test_file" --reporter expanded --timeout 30s
          done

      - name: Compilar APK
        run: flutter build apk --release

      - name: Renombrar APK
        run: cp build/app/outputs/flutter-apk/app-release.apk Velyntora-Android-v0.1.0.apk

      - name: Publicar APK
        uses: actions/upload-artifact@v5
        with:
          name: Velyntora-Android-v0.1.0
          path: Velyntora-Android-v0.1.0.apk
          if-no-files-found: error
          retention-days: 30
