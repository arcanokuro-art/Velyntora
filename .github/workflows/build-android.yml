name: Compilar Velyntora APK

permissions:
  contents: write

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-android:
    timeout-minutes: 20
    runs-on: ubuntu-latest
    steps:
      - name: Descargar código
        uses: actions/checkout@v5

      - name: Instalar Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Crear soporte de Android
        run: flutter create . --platforms=android --project-name=velyntora --org=art.arcanokuro

      - name: Descargar dependencias
        run: flutter pub get

      - name: Aplicar icono de Velyntora
        run: dart run flutter_launcher_icons

      - name: Verificar formato
        run: dart format --output=none --set-exit-if-changed lib test

      - name: Analizar código
        run: flutter analyze

      - name: Ejecutar pruebas
        run: |
          for test_file in test/*_test.dart; do
            echo "Ejecutando $test_file"
            timeout 90s flutter test "$test_file" --reporter expanded --timeout 30s
          done

      - name: Compilar APK
        run: flutter build apk --release

      - name: Renombrar APK
        run: cp build/app/outputs/flutter-apk/app-release.apk Velyntora-Android-v0.1.0.apk

      - name: Publicar APK
        uses: actions/upload-artifact@v7
        with:
          name: Velyntora-Android-v0.1.0
          path: Velyntora-Android-v0.1.0.apk
          if-no-files-found: error
          retention-days: 30

      - name: Crear GitHub Release v0.1.0
        if: >-
          github.event_name == 'push' &&
          contains(github.event.head_commit.message, 'release: publish v0.1.0')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v0.1.0
          name: Velyntora v0.1.0
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          make_latest: true
          files: Velyntora-Android-v0.1.0.apk
